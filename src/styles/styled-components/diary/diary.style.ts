'use client';

import styled from "styled-components";

// export const MonthlyDiaryImg = styled.div<{ $preview?: string }>`
//   ${({ $preview }) => ($preview ? `background: url(${$preview}) no-repeat center/cover;` : '')}
//   backgorund: url('')
//   width: 80%;
//   aspect-ratio: 16 / 9;
//   display: grid;
//   place-items: center;
//   background-color: #e3e3e3;
//   border-radius: 15px;
//   cursor: pointer;
//   margin: 5px 0px;
// `;

export const MonthlyDiaryImg = styled.img`
    width: 100%;
    border-radius: 15px;
`;

